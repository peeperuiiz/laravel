<header class="bg-blue-600 flex justify-between px-[50px] py-5">
    <div class="w-auto">
        <span class="text-gray-300 text-3xl font-bold">LARAVEL</span>
    </div>
    <nav class="w-[50%] flex justify-between items-center text-gray-300 font-semibold">
        <a href="{{route('home')}}">Home</a>
        <a href="{{route('about')}}">About</a>
        <a href="{{route('contact')}}">Contact</a>
        <a href="{{route('services')}}">Services</a>
    </nav>
</header>